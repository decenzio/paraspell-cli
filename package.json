{
  "name": "paraspell-cli-monorepo",
  "private": true,
  "version": "1.0.0",
  "description": "ParaSpell CLI monorepo — publishable package is in cli/ (create-paraspell)",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "build": "npm run build --prefix cli",
    "test": "npm test --prefix cli"
  }
}
